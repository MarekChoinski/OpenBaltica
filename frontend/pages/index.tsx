import { NextPage } from "next";
import { PinataUploadForm } from "../components/PinataUploadForm/PinataUploadForm";

const Home: NextPage = () => {
  return (
    <div>
      <PinataUploadForm />
    </div>
  );
};

export default Home;
